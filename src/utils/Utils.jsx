
/* The possible state action types available to us, edit this object to add or remove a state action type */
const actionTypes = {
    SET_THEME : "SET_THEME", // set the appTheme (dark===true, light===false)
    OPEN_DRAWER: "OPEN_DRAWER", // open the side bar (opened===true, closed===false)
    SET_DATA: "SET_DATA", // open the side bar (opened===true, closed===false)
    SET_LOADING: "SET_LOADING", // open the side bar (opened===true, closed===false)
}

export default actionTypes;